parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        composer: []
        phplint: []
        phan: []
        phpstan: []
        composer_require_checker: []
        phpcs:
            standard: PSR2
        phpmd:
            ruleset: ['cleancode', 'codesize', 'design', 'unusedcode']
        phpunit: []
        infection:
            threads: 4
